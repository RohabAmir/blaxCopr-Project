
export type Nav = {
    title: string;
    link: string;
}

export enum ButtonType {
    Primary = 'primary',
    Secondary = 'secondary'
}
export enum ButtonSize {
    Large = 'large',
    Medium = 'medium',
    Small = "small"
}
export enum IconType {
    BackArrow = 'back-arrow',
    Filter = "filter",
    None = "none"
}
